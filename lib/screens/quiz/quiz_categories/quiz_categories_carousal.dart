import 'package:flutter/material.dart';

class QuizCategoriesCarousal extends StatelessWidget {
  QuizCategoriesCarousal({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
